# 豆包 Recipe

Route: `source.doubao`. Current status is **disabled**. Stop formal collection unless the runtime
capability has been explicitly changed to `enabled` by later evidence.

## What Is Proven

- An Agent has traversed real history, selected one bounded conversation, acquired structured text,
  saved it through unified C0, and observed an unchanged recollection.
- The complex conversation “战略领导力W1” has seven verified original DOCX attachments and one PDF
  preview in C0.
- Attaching the recovered originals used generic Rust `capture attach-assets` and changed no body
  revision or C1 records.

These are case evidence, not a `babata-doubao-collect` Skill and not proof of a generally repeatable
source recipe.

## Remaining Gap

Only that conversation's DOCX shape is closed. Other attachment/media shapes, a formal controlled
Agent route, and long-term recollection are not complete. The source therefore remains disabled.

When enabled later, keep the same `source.doubao` recipe and extend its content-shape matrix. A
request such as “collect W1” must still resolve the exact conversation and attachment scope; it may
not expand to all logged-in history. Never interpret the downloaded documents or start C1 as part of
collection.
