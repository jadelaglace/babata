interface:
  display_name: "Babata 收集"
  short_description: "Recover scoped source data and register it as C0-C when available"
  default_prompt: "Use $babata-collect to collect one explicitly scoped source into Babata C0 and report the real route limitations."
